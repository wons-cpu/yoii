/** Visual spec colors — see CLAUDE.md "Visual spec". Dark map, never light. */
export const PALETTE = {
  background: '#12110F',
  roadGeometry: '#221F1C',
  barMark: '#F2EEE5',
  primaryText: '#F2EEE5',
  secondaryText: '#8A8579',
  /** Fallback route color if a phrase's track is missing its colors. */
  routePlaceholder: '#F2EEE5',
} as const;
